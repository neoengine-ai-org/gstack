name: Workflow Lint
on: [push, pull_request]
jobs:
  actionlint:
    runs-on: ${{ vars.GSTACK_LINUX_RUNNER || 'ubuntu-latest' }}
    steps:
      - uses: actions/checkout@v4
      - uses: rhysd/actionlint@v1.7.11
